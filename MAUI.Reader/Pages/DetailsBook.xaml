<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI.Reader.Pages.DetailsBook"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MAUI.Reader.Pages"
             xmlns:vm="clr-namespace:MAUI.Reader.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="vm:InDesignReadBook"
             x:DataType="vm:DetailsBook"
             Title="DetailsBook">
    
    
   
    

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Text="{Binding CurrentBook.Name}" Margin="5"/>

        <Label Grid.Row="1" Text="{Binding CurrentBook.Author}" Margin="5"/>
        <Label Grid.Row="2" Text="{Binding CurrentBook.Price}" Margin="5"/>
        <Label Grid.Row="3" Text="{Binding CurrentBook.Description}" Margin="5"/>

        <Button Grid.Row="4" Text="Lire le livre" Command="{Binding ReadBook2Command}"/>
    </Grid>

</ContentPage>